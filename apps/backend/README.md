# Backend API — AI Insurance Agent

Python + FastAPI. Main REST API layer: auth, wallet, policies, renewals, claims, applications. Talks to PostgreSQL (Neon), NoSQL, and internal services (chatbot, voice agent, automation) via clients.

Structure only — no implementation yet. See root `docs/ARCHITECTURE.md`.
