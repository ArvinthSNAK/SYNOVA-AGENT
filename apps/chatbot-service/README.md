# Chatbot Service — AI Insurance Agent

Python + FastAPI. Dedicated conversational agent service: intent recognition, context/session management, structured data collection for insurance workflows.

Structure only — no implementation yet. See root `docs/ARCHITECTURE.md`.
