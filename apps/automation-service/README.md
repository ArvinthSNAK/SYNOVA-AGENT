# Automation Service — AI Insurance Agent

Python + Playwright. Isolated, controlled browser automation layer for authorized third-party insurance form filling, gated by explicit user confirmation.

Structure only — no implementation yet. See root `docs/ARCHITECTURE.md`.
